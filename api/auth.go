package api

// Handle authentication here.
// There’s no need to create a separate auth package for JWT/OAuth authentication.
// Keeping everything inside the API layer is much simpler and reduces the chances of circular dependencies.
